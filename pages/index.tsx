import React from "react";

import { Container, Header, Footer, Cards } from "@components";
import {Main} from "../src/container/login/index"
const Home: React.FC = () => {
  return (
    <Container>
      <Main />
    </Container>
  );
};

export default Home;
